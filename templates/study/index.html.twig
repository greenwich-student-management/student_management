{% extends 'admin.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
                    <h3>Manager</h3> <a href="{{path('study_create')}}"><button type="button" class="btn btn-outline-primary">Insert</button></a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Term</th>
                            <th>Part</th>
                            <th>Subject</th>
                            <th>Class</th>
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for stu in study %}
                              <tr>
                                  <td>{{ stu.id}}</td>
                                  <td>{{ stu.Term}}</td>
                                  <td>{{ stu.part }}</td
                                  <td>{{ stu.subject}}</td>
                                  <td>{{ stu.class }}</td>

                                  <td>
                                    <a href="{{path('study_edit',{'id':stu.id})}}">Edit</a>
                                    <a href="{{path('study_delete',{'id':stu.id})}}">Delete</a></td>
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}
