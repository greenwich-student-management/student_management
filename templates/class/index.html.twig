{% extends 'admin.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
                   <a style="text-decoration:none; color:#FF9900;font-size:40px; " <h3>Classroom Manager </h3> </a>
                   </div>
                    <a href="{{path('score_create')}}">
                    
                      <button class="btn btn-lg btn-primary" type="button"
                style=" 
                margin-left: 50px; 
                height: 50px;
                text-align: center;
                margin-top: 20px">
                 Insert
                  </button>
                  </a>  
                <div class="page-content">
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Class Name</th>
                            <th>Class Code</th>
                            <th>Major</th>
                            <th>Action</th>
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for c in class %}
                              <tr>
                                  <td>{{ c.id}}</td>
                                  <td>{{ c.classname}}</td>
                                  <td>{{ c.classcode}}</td>
                                  <td>{{ c.major}}</td>
                                  <td>
                                    <a href="{{path('class_edit',{'id':c.id})}}">Edit</a>
                                    <a href="{{path('class_delete',{'id':c.id})}}">Delete</a></td>
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}
